export enum BODYGUARD_MESSAGE {
  CREATE = "bodyguard.create",
  FINDALL = "bodyguard.find_all",
  UPDATE = "bodyguard.update",
  FINDONE = "bodyguard.find_one",
  CREATE_AND_SAVE = "bodyguard.create_and_save"
}

export enum BODYGUARD_TRANNING_MESSAGE {
  CREATE = "bodyguard_tranning.create",
  FINDALL = "bodyguard_tranning.find_all",
  UPDATE = "bodyguard_tranning.update",
  FINDONE = "bodyguard_tranning.find_one",
  CREATE_AND_SAVE = "bodyguard_tranning.create_and_save"
}

export enum SALARY_MESSAGE {
  CREATE = "salary.create",
  FINDALL = "salary.find_all",
  UPDATE = "salary.update",
  FINDONE = "salary.find_one",
  CREATE_AND_SAVE = "salary.create_and_save"
}

export enum JOB_MESSAGE {
  CREATE = "job.create",
  FINDALL = "job.find_all",
  UPDATE = "job.update",
  FINDONE = "job.find_one",
  CREATE_AND_SAVE = "job.create_and_save"
}

export enum SKILL_MESSAGE {
  CREATE = "skill.create",
  FINDALL = "skill.find_all",
  UPDATE = "skill.update",
  FINDONE = "skill.find_one",
  CREATE_AND_SAVE = "skill.create_and_save"
}

export enum TIMESHEET_MESSAGE {
  CREATE = "timesheet.create",
  FINDALL = "timesheet.find_all",
  UPDATE = "timesheet.update",
  FINDONE = "timesheet.find_one",
  CREATE_AND_SAVE = "timesheet.create_and_save"
}

export enum TRANNING_CATALOG {
  CREATE = "tranning_catalog.create",
  FINDALL = "tranning_catalog.find_all",
  UPDATE = "tranning_catalog.update",
  FINDONE = "tranning_catalog.find_one",
  CREATE_AND_SAVE = "tranning_catalog.create_and_save"
}

